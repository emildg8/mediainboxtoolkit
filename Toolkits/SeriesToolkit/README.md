# SeriesToolkit (legacy slot)

Это целевой слот новой структуры `MediaInboxToolkit/Toolkits`.

Текущее рабочее расположение исходников и релизной линии остаётся:

- `..\..\..\SeriesToolkit\`

Назначение слота:

1. зафиксировать, что `SeriesToolkit` входит в семейство инструментов `MediaInboxToolkit`;
2. подготовить безопасный поэтапный перенос (launcher-совместимость, пути, CI);
3. хранить будущие адаптеры/обёртки, если потребуется совместный запуск из единой оболочки.
